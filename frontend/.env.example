# URL base de la API. Solo se necesita en builds de producción.
# En desarrollo (npm run dev), déjala vacía: Vite usa su proxy a localhost:5000.
# Ejemplo producción: VITE_API_URL=https://nutrigestion-api.onrender.com/api
VITE_API_URL=
